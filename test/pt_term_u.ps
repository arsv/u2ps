(pt.ps) run
/unifont/ProcSet findresource { def } forall
/uniterm/ProcSet findresource { def } forall
/unidata/ProcSet findresource { def } forall

% /u is used to show proper utf8 strings.

/currentfont << /CharStrings <<
	/a 1
	/b 2
	/c 3
	/d 4
>> >> def

/glyphshow { pop } def
(T1): (abc) u :: TEST

/glyphshow { count 1 roll } def
(T2): (abc) u :: /c /b /a TEST
